# PLAN_I_UPDATE.md — Phase I: Later (not scheduled)

> Status: **backlog** · Prio P3. No detailed spec yet (no `SPECS_I_UPDATE.md` until scoped).
> Index: `PLAN_UPDATE.md`.

Kept on the roadmap, **not specced** until A–H land:

- **Module registry** (read-only) — browse/pin internal modules.
- **Outbound run-tasks / enriched webhooks** — integrations on run lifecycle.
- **SSO / SAML** — enterprise auth beyond Google OIDC.
- **GitHub App** (bot identity + Checks API) — the richer VCS integration deferred from Phase A.

Each becomes its own `PLAN_*_UPDATE.md` + `SPECS_*_UPDATE.md` when promoted.
